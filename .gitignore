# Claude Code local settings
.claude/

# OS files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/

# Reference photos with real license plates — used locally to calibrate
# guideTemplates.ts, must not be pushed to this public repo
golden_photos/
test_pic/

# Raw local model training artifacts (best.pt/.tflite, working files) — only the
# exported TFJS zip (專案/car_plate_ocr/) is kept in the repo as a backup, mirroring
# 專案/car_yolo/yolov8_tfjs_model.zip
data/
專案/car_plate_ocr/*.pt
專案/car_plate_ocr/*.onnx
